version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current. Dependabot bumps both the SHA
  # and the trailing `# vX.Y.Z` comment, so pinning costs no manual maintenance.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
